spring:
  application:
    name: Sistema de cadastro QRcode
  datasource:
    url: jdbc:h2:mem:funcionarios
    driver-class-name: org.h2.Driver
    username: zerbieti
    password: zerbieti1
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect